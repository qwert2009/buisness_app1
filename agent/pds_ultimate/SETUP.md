# 🚀 PDS-Ultimate — Что нужно для запуска

## ⚡ Обязательные (система НЕ запустится без них)

### 1. Telegram Bot Token
- Открой [@BotFather](https://t.me/BotFather) в Telegram
- Отправь `/newbot`
- Дай имя боту (например: `PDS Ultimate`)
- Дай username (например: `pds_ultimate_bot`)
- Скопируй токен → `.env` → `TG_BOT_TOKEN=`

### 2. Твой Telegram User ID
- Открой [@userinfobot](https://t.me/userinfobot) в Telegram
- Он покажет твой ID (число)
- Скопируй → `.env` → `TG_OWNER_ID=`

### 3. DeepSeek API Key
- Зайди на [platform.deepseek.com](https://platform.deepseek.com/)
- Зарегистрируйся → API Keys → Create
- Скопируй ключ → `.env` → `DEEPSEEK_API_KEY=`
- **Пополни баланс** (минимум $5 для старта)

---

## 🔧 Рекомендуемые (для полного функционала)

### 4. Telethon (анализ стиля TG — мимикрия)
- Зайди на [my.telegram.org/apps](https://my.telegram.org/apps)
- Авторизуйся по номеру телефона
- Создай приложение (название: любое, platform: Other)
- Скопируй `api_id` → `.env` → `TG_API_ID=`
- Скопируй `api_hash` → `.env` → `TG_API_HASH=`
- При первом запуске система попросит код из Telegram

### 5. Gmail API (отчёты на почту каждые 3 дня)
- Зайди в [Google Cloud Console](https://console.cloud.google.com/)
- Создай проект → Включи Gmail API
- OAuth 2.0 → Скачай `credentials.json`
- Положи файл → `pds_ultimate/credentials/gmail.json`
- `.env` → `GMAIL_ENABLED=true`
- `.env` → `GMAIL_OWNER_EMAIL=твоя_почта@gmail.com`
- При первом запуске откроется браузер для авторизации

### 6. WhatsApp (анализ стиля WA)
- `.env` → `WA_ENABLED=true`
- `.env` → `WA_HEADLESS=false` (для первого запуска — QR-код)
- При первом запуске отсканируй QR-код в браузере
- Потом можно поставить `WA_HEADLESS=true`

---

## 💰 Финансовые настройки

### 7. Распределение прибыли
В `.env`:
```
FINANCE_EXPENSE_PERCENT=50.0    # % на расходы
FINANCE_SAVINGS_PERCENT=50.0    # % на отложения
```
Сумма должна быть = 100%.

### 8. Валюты (фиксированные курсы)
Уже настроено в коде:
- 1 USD = 19.5 TMT (Туркменский манат)
- 1 USD = 7.1 CNY (Китайский юань)

Если курсы изменятся — поменять в `config.py` → `CurrencyConfig.fixed_rates`.

---

## 🛡️ Безопасность

### 9. Кодовое слово для экстренного удаления
- `.env` → `SECURITY_EMERGENCY_CODE=твоё_секретное_слово`
- Отправь это слово боту → он удалит все финансовые данные

### 10. Бэкап на почту (опционально)
```
SECURITY_BACKUP_TARGET=email
SECURITY_BACKUP_EMAIL=вторая_почта@gmail.com
```

---

## 📋 Минимальный .env файл

```dotenv
# === ОБЯЗАТЕЛЬНЫЕ ===
TG_BOT_TOKEN=123456:ABC-DEF...
TG_OWNER_ID=987654321
DEEPSEEK_API_KEY=sk-...

# === РЕКОМЕНДУЕМЫЕ ===
TG_API_ID=12345678
TG_API_HASH=abcdef123456...

# === ФИНАНСЫ ===
FINANCE_EXPENSE_PERCENT=50.0
FINANCE_SAVINGS_PERCENT=50.0

# === БЕЗОПАСНОСТЬ ===
SECURITY_EMERGENCY_CODE=мой_секрет
```

---

## 🖥️ Запуск

```bash
# 1. Клонировать
git clone https://github.com/qwert2009/buisness_app1.git
cd buisness_app1/agent

# 2. Создать виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate

# 3. Установить зависимости
pip install -r pds_ultimate/requirements.txt

# 4. Скопировать и заполнить .env
cp pds_ultimate/.env.example pds_ultimate/.env
nano pds_ultimate/.env    # заполнить значения

# 5. Запустить
python -m pds_ultimate.main
```

---

## ✅ Контрольный чек-лист

| # | Что нужно | Где взять | Куда вставить |
|---|-----------|-----------|---------------|
| 1 | Bot Token | @BotFather | `TG_BOT_TOKEN` |
| 2 | User ID | @userinfobot | `TG_OWNER_ID` |
| 3 | DeepSeek Key | platform.deepseek.com | `DEEPSEEK_API_KEY` |
| 4 | TG API ID | my.telegram.org | `TG_API_ID` |
| 5 | TG API Hash | my.telegram.org | `TG_API_HASH` |
| 6 | Gmail JSON | Google Cloud Console | `credentials/gmail.json` |
| 7 | Gmail Email | — | `GMAIL_OWNER_EMAIL` |
| 8 | Кодовое слово | Придумать самому | `SECURITY_EMERGENCY_CODE` |

**Минимум для старта: пункты 1, 2, 3** — система запустится и будет работать.
Остальное — для расширенного функционала (мимикрия стиля, отчёты на почту).
