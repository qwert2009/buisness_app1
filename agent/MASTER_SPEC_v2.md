# 🏗️ МАСТЕР-СПЕЦИФИКАЦИЯ v2.0 — СИСТЕМА «PDS-ULTIMATE»
## Обновлено: 2026-02-08 | Статус: УТВЕРЖДЕНО

---

## 📋 ВНЕСЁННЫЕ ПРАВКИ ВЛАДЕЛЬЦА

| # | Правка | Статус |
|---|--------|--------|
| 1 | Whisper — ЛОКАЛЬНЫЙ (Faster-Whisper), DeepSeek — через API | ✅ Принято |
| 2 | Анализ стиля: WhatsApp — 3 чата, Telegram — 7 чатов | ✅ Принято |
| 3 | Финансовая логика: сначала «сколько заплатили МНЕ», потом «сколько заплатил Я поставщику» → остаток → минус доставка → чистая прибыль (БЕЗ налогов и комиссий) | ✅ Принято |
| 4 | Чистая прибыль → распределение: Расходы + Отложения на будущее | ✅ Принято |
| 5 | Агент выполняет ЛЮБЫЕ задачи, не только из ТЗ | ✅ Принято |
| 6 | Курсы: 1 USD = 19.5 TMT, 1 USD = 7.1 CNY, остальное — из интернета | ✅ Принято |
| 7 | Все закрытые заказы → сохранение ВСЕХ позиций в единый архивный файл (история за всё время) | ✅ Принято |
| 8 | Агент умеет создавать новые файлы, столбцы, вести учёт в произвольных форматах | ✅ Принято |

---

## 1. ОБЩАЯ КОНЦЕПЦИЯ

Автономная AI-экосистема на Python, управляемая через Telegram.
Заменяет: логиста, бухгалтера, личного секретаря.
**Дополнение**: Агент — универсальный исполнитель. Он выполняет ЛЮБУЮ задачу, которую ему поставит владелец, даже если она не описана в ТЗ (поиск информации, генерация текстов, расчёты, работа с файлами любых форматов, и т.д.).

### 1.1. Технологическое ядро

| Компонент | Технология | Примечание |
|-----------|-----------|------------|
| LLM (мозг) | **DeepSeek API** (cloud) | Оплата по токенам, максимальное качество |
| Голос | **Faster-Whisper** (локально) | Бесплатно, работает на GPU/CPU сервера |
| Telegram Bot | Aiogram 3.x | Основной интерфейс |
| Telegram Userbot | Telethon | Чтение чатов, мимикрия |
| WhatsApp | Playwright | Браузерная автоматизация |
| БД | SQLAlchemy + SQLite | Локально, на сервере владельца |
| Excel | XlsxWriter + Pandas + openpyxl | Создание + чтение + редактирование |
| Планировщик | APScheduler | Отказоустойчивый |
| Почта | Gmail API | Чтение + отправка + отчёты |
| Календарь | Google Calendar API | Управление встречами |
| OCR | Tesseract / EasyOCR | Распознавание фото чеков, накладных |
| Конвертация | python-docx, PyPDF2, Pillow | Word, PDF, изображения |

---

## 2. МОДУЛЬ «ЛИЧНЫЙ СЕКРЕТАРЬ И КОММУНИКАЦИИ»

### 2.1. Инициализация и Мимикрия

- При первом запуске агент сканирует:
  - **Telegram**: последние **7 активных чатов** (через Telethon)
  - **WhatsApp**: последние **3 активных чата** (через Playwright)
- DeepSeek анализирует: длину сообщений, сленг, эмодзи, официальность, манеру
- Формирует **«Communication Style Guide»** — профиль стиля владельца
- Все исходящие сообщения генерируются в этом стиле
- Периодическое обновление профиля (раз в неделю пересканирование)

### 2.2. VIP-HUB (Фильтрация и Приоритеты)

- **White List**: Список приоритетных контактов (email, TG, WA)
- **Smart Alert**: Сообщения от VIP → мгновенное саммари + предлагаемый ответ
- **Авто-ответчик**: Интеграция с Google Calendar. Если занят → «Я на встрече до 15:00, отвечу позже»
- **Почтовый агент**: Чтение + ответы на Gmail в стиле владельца

### 2.3. Календарь и Заметки

- **Event Manager**: Создание встреч из текста/голоса
- **Конфликт-менеджер**: Проверка наложений, предложение альтернатив, предупреждение «нет времени на обед»
- **Умные карточки контрагентов**: Голосовое «Этот поставщик жулик» → карточка в БД → предупреждение при следующем заказе
- **Гео-учёт**: Адрес → навигатор за 15 минут до выезда

---

## 3. МОДУЛЬ «ЛОГИСТИКА И УПРАВЛЕНИЕ ЗАКАЗАМИ»

### 3.1. Фаза 1: Создание заказа (Input)

**Ввод данных:**
- Файл: Excel, Word, PDF, фото накладной (OCR)
- Текст: «Балаклавы 100 шт, цена 200$»
- Голос: Faster-Whisper → текст → парсинг
- DeepSeek + regex для 100% точности парсинга
- Мгновенная склейка: файл + текстовые дополнения → один заказ

**Верификация:**
- Вывод списка позиций с кнопками «Изменить»
- После «Готово» → создание Временного Excel-файла заказа

**💰 Финансовый старт (ОБНОВЛЁННАЯ ЛОГИКА):**

```
Шаг 1: «Сколько вам ЗАПЛАТИЛИ за этот заказ?» → фиксируется как ДОХОД
Шаг 2: «Сколько вы ЗАПЛАТИЛИ поставщику за товар?» → фиксируется как РАСХОД_ТОВАР
Шаг 3: ОСТАТОК = ДОХОД - РАСХОД_ТОВАР
Шаг 4: (после доставки) ЧИСТАЯ_ПРИБЫЛЬ = ОСТАТОК - РАСХОД_ДОСТАВКА
Шаг 5: Распределение ЧИСТОЙ ПРИБЫЛИ:
        → На расходы (% задаётся владельцем)
        → Отложения на будущее (% задаётся владельцем)
```

**❌ НЕ считаем:** налоги, комиссии

### 3.2. Фаза 2: Сопровождение (Tracking)

- **T+4 дня**: Бот спрашивает по КАЖДОЙ позиции: «Позиция #1 (Балаклавы) пришла?»
- **Если НЕТ**: Повтор каждый вторник (задача в календарь)
- **Если ДА**: Запрос трек-номера. Фото → OCR → авто-извлечение номера
- **Анти-забывание**: Не ответили → напомнит через 2 часа, потом вечером

### 3.3. Фаза 3: Закрытие (Finalizing)

- Когда все позиции «Прибыло» → вопрос: «Доставку по каждой позиции или общей суммой?»
- Если общей → распределение пропорционально (по весу или цене)
- Расчёт чистой прибыли (см. формулу выше)
- Распределение по правилам владельца

**📦 АРХИВАЦИЯ (НОВОЕ):**
```
1. Все позиции закрытого заказа → копируются в ЕДИНЫЙ АРХИВНЫЙ ФАЙЛ
   (All_Orders_Archive.xlsx) — хранит ВСЕ заказы за ВСЁ время
2. Только после успешного сохранения в архив → временный файл удаляется
3. Итоговые суммы → переносятся в Master_Finance.xlsx
```

---

## 4. ФИНАНСОВОЕ ЯДРО (Financial Engine)

### 4.1. Структура файлов

| Файл | Назначение | Удаляется? |
|------|-----------|-----------|
| `Master_Finance.xlsx` | Главная книга: баланс, прибыль, расходы, отложения | ❌ НИКОГДА |
| `All_Orders_Archive.xlsx` | ВСЕ позиции ВСЕХ заказов за всё время | ❌ НИКОГДА |
| `Order_XXXX_temp.xlsx` | Временный файл активного заказа | ✅ После закрытия |

### 4.2. Master Finance — Разделы

- **Оборот**: Все поступления
- **Расходы на товар**: Все выплаты поставщикам
- **Расходы на доставку**: Логистика
- **Чистая прибыль**: Оборот - Товар - Доставка
- **На расходы**: % от чистой прибыли (настраивается)
- **Отложения на будущее**: % от чистой прибыли (настраивается)
- **Личные расходы**: Чеки, покупки (сканер чеков)

### 4.3. Мультивалютность

| Валютная пара | Курс | Источник |
|--------------|------|---------|
| 1 USD → TMT | **19.5** | Фиксированный |
| 1 USD → CNY | **7.1** | Фиксированный |
| Все остальные | Динамический | API (exchangerate-api.com) |

### 4.4. Sync Logic (Файл-как-Эталон)

- Владелец скачивает Excel → правит руками → скидывает обратно
- Бот удаляет старые данные в БД → перезаписывает из файла
- Файл = ЭТАЛОН, БД подстраивается

---

## 5. УНИВЕРСАЛЬНЫЙ ФАЙЛОВЫЙ МЕНЕДЖЕР (НОВОЕ)

### 5.1. Создание файлов

Агент умеет создавать файлы **любых форматов** по запросу:
- **Excel** (.xlsx): Таблицы с формулами, форматированием, графиками
- **Word** (.docx): Документы, письма, контракты
- **PDF**: Инвойсы, упаковочные листы, отчёты
- **CSV**: Простые таблицы для импорта/экспорта
- **TXT / Markdown**: Заметки, инструкции
- **JSON**: Структурированные данные

### 5.2. Работа с существующими файлами

- Открыть любой файл → прочитать → внести изменения
- Добавить новые столбцы / строки / листы в Excel
- Вести произвольный учёт (не только заказы)
- Объединять данные из нескольких файлов
- Конвертировать между форматами (Word → PDF, Excel → CSV, и т.д.)

### 5.3. Примеры использования

```
Пользователь: «Создай таблицу учёта сотрудников»
→ Бот создаёт Excel с колонками: ФИО, Должность, Зарплата, Дата найма...

Пользователь: «Добавь в файл расходов колонку 'Категория'»
→ Бот открывает файл, добавляет столбец, сохраняет

Пользователь: «Сделай PDF-инвойс для заказа #5»
→ Бот генерирует профессиональный PDF
```

---

## 6. СВОБОДНЫЙ РЕЖИМ (UNIVERSAL AGENT) — НОВОЕ

Агент — **не только секретарь/логист**. Он выполняет ЛЮБУЮ задачу:

### 6.1. Что умеет вне ТЗ

- 🔍 **Поиск информации**: Найти поставщика, узнать цену, исследовать рынок
- 📝 **Генерация текстов**: Письма, коммерческие предложения, посты
- 🧮 **Произвольные расчёты**: Любая математика, конвертация, анализ
- 💡 **Консультации**: Совет по бизнесу, логистике, стратегии
- 🌐 **Перевод**: Любые языки в реальном времени
- 📊 **Аналитика**: Анализ данных, прогнозы, тренды
- 🖼️ **Работа с изображениями**: OCR, описание, извлечение данных
- 📁 **Файловые операции**: Создание, редактирование, конвертация любых файлов

### 6.2. Как это работает

```
Пользователь: (любой запрос, не связанный с заказами)
→ DeepSeek API определяет intent (намерение)
→ Если это задача из ТЗ → запускается соответствующий модуль
→ Если это свободная задача → DeepSeek выполняет напрямую
→ Результат возвращается в чат
```

---

## 7. ЭКСКЛЮЗИВНЫЕ ФУНКЦИИ (Executive Tools)

### 7.1. Утренний брифинг (08:30)
```
📋 БРИФИНГ НА 08.02.2026

📅 Встречи: 3 (первая в 10:00)
📦 Ожидаем: 2 посылки (Балаклавы — трек CN123, Маски — без трека)
💰 Баланс: $5,430 | Отложено: $2,100
🌤️ Гуанчжоу: +18°, ясно | Задержек рейсов нет
⚠️ Напоминание: Поставщик Ли не ответил 3 дня
```

### 7.2. Языковой мост
- Авто-перевод WhatsApp: Китайский ↔ Русский ↔ Английский ↔ Туркменский

### 7.3. Генератор документов
- PDF-инвойсы, упаковочные листы по команде

### 7.4. Сканер чеков
- Фото чека → OCR → сумма + категория → «Личные расходы»

### 7.5. Архивариус
- Все файлы переименовываются: `2026_02_07_Заказ_Балаклавы.pdf`

### 7.6. Security Mode
- Кодовое слово → мгновенное удаление финансовых файлов

### 7.7. Авто-бэкап
- Ежесуточный бэкап БД + Excel → облако или вторая почта

---

## 8. ОТЧЁТНОСТЬ

- **Каждые 3 дня (09:00)**: Полный Excel-отчёт → Gmail
- Содержимое: движение средств, статус заказов, прогноз прибыли
- XlsxWriter-форматирование: цвета, формулы, графики, закреплённые области

---

## 9. ПОЛНЫЙ ТЕХНИЧЕСКИЙ СТЕК (ФИНАЛ)

```
Python 3.11+
├── Core
│   ├── asyncio (асинхронность)
│   ├── SQLAlchemy + SQLite (БД)
│   └── APScheduler (планировщик)
├── AI
│   ├── DeepSeek API (LLM — облако)
│   └── Faster-Whisper (голос — локально)
├── Messengers
│   ├── Aiogram 3.x (TG Bot)
│   ├── Telethon (TG Userbot)
│   └── Playwright (WhatsApp)
├── Files
│   ├── XlsxWriter (создание Excel)
│   ├── openpyxl (чтение/редактирование Excel)
│   ├── Pandas (анализ данных)
│   ├── python-docx (Word)
│   ├── PyPDF2 / reportlab (PDF)
│   └── Pillow (изображения)
├── OCR
│   ├── Tesseract / EasyOCR
│   └── (для чеков, накладных, трек-номеров)
├── Google
│   ├── Gmail API
│   └── Google Calendar API
├── Валюты
│   ├── Фиксированные: USD/TMT=19.5, USD/CNY=7.1
│   └── exchangerate-api.com (остальные)
└── Безопасность
    ├── Ежесуточный бэкап
    └── Security Mode (экстренное удаление)
```

---

## 11. АРХИТЕКТУРА ПРОЕКТА (СТРУКТУРА ПАПОК)

```
pds_ultimate/
├── main.py                     # Точка входа
├── config.py                   # Конфигурация (токены, курсы, %)
├── requirements.txt            # Зависимости
│
├── core/                       # Ядро системы
│   ├── __init__.py
│   ├── database.py             # SQLAlchemy модели + движок
│   ├── scheduler.py            # APScheduler задачи
│   └── llm_engine.py           # DeepSeek API интерфейс
│
├── modules/
│   ├── __init__.py
│   ├── secretary/              # Модуль секретаря
│   │   ├── __init__.py
│   │   ├── style_analyzer.py   # Мимикрия стиля
│   │   ├── vip_hub.py          # White List + фильтрация
│   │   ├── auto_responder.py   # Авто-ответчик
│   │   └── calendar_mgr.py    # Календарь + конфликты
│   │
│   ├── logistics/              # Модуль логистики
│   │   ├── __init__.py
│   │   ├── order_manager.py    # Жизненный цикл заказа
│   │   ├── item_tracker.py     # Трекинг позиций
│   │   ├── delivery_calc.py    # Расчёт доставки
│   │   └── archive.py          # Архивация в All_Orders
│   │
│   ├── finance/                # Финансовое ядро
│   │   ├── __init__.py
│   │   ├── master_finance.py   # Главная книга
│   │   ├── currency.py         # Курсы валют
│   │   ├── profit_calc.py      # Расчёт прибыли
│   │   └── sync_engine.py      # Файл-как-Эталон
│   │
│   ├── files/                  # Файловый менеджер
│   │   ├── __init__.py
│   │   ├── file_manager.py     # Создание/редактирование файлов
│   │   ├── excel_engine.py     # XlsxWriter + openpyxl
│   │   ├── pdf_engine.py       # PDF генерация
│   │   ├── ocr_engine.py       # OCR (чеки, накладные)
│   │   └── converter.py        # Конвертация форматов
│   │
│   └── executive/              # Эксклюзивные функции
│       ├── __init__.py
│       ├── morning_brief.py    # Утренний брифинг
│       ├── translator.py       # Языковой мост
│       ├── receipt_scanner.py  # Сканер чеков
│       ├── archivist.py        # Архивариус (переименование)
│       ├── security.py         # Экстренная блокировка
│       └── backup.py           # Авто-бэкап
│
├── bot/                        # Telegram интерфейс
│   ├── __init__.py
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── commands.py         # /start, /status, /help
│   │   ├── orders.py           # Обработка заказов
│   │   ├── finance.py          # Финансовые команды
│   │   ├── files.py            # Файловые команды
│   │   ├── voice.py            # Голосовые сообщения
│   │   └── universal.py        # Свободный режим (любые задачи)
│   ├── keyboards.py            # Inline-клавиатуры
│   └── middlewares.py          # Middleware (логирование, авторизация)
│
├── integrations/               # Внешние интеграции
│   ├── __init__.py
│   ├── whatsapp.py             # Playwright WA
│   ├── gmail.py                # Gmail API
│   ├── google_calendar.py      # Calendar API
│   └── exchange_rates.py       # Курсы валют API
│
├── data/                       # Данные (создаётся автоматически)
│   ├── pds_ultimate.db         # SQLite база
│   ├── Master_Finance.xlsx     # Главная книга
│   ├── All_Orders_Archive.xlsx # Архив ВСЕХ заказов
│   ├── active_orders/          # Временные файлы заказов
│   ├── documents/              # Сгенерированные документы
│   ├── backups/                # Бэкапы
│   └── user_files/             # Произвольные файлы пользователя
│
└── utils/                      # Утилиты
    ├── __init__.py
    ├── parsers.py              # Парсеры (текст, файлы)
    ├── formatters.py           # Форматирование сообщений
    ├── validators.py           # Валидация данных
    └── helpers.py              # Общие хелперы
```

---

## 12. РЕЗЮМЕ ГЛАВНОГО ИНЖЕНЕРА

### Что имеем в итоге:

1. **DeepSeek API** (облако) — максимальное качество LLM
2. **Faster-Whisper** (локально) — бесплатная обработка голоса
3. **Стиль**: 7 чатов TG + 3 чата WA для мимикрии
4. **Финансы**: ДОХОД → минус ТОВАР → минус ДОСТАВКА → ЧИСТАЯ ПРИБЫЛЬ → Расходы + Отложения
5. **Без налогов и комиссий** в расчётах
6. **Архив**: ВСЕ заказы за ВСЁ время в одном файле
7. **Универсальный агент**: Выполняет ЛЮБУЮ задачу
8. **Файловый менеджер**: Создаёт/редактирует файлы любых форматов
9. **Фиксированные курсы**: USD/TMT = 19.5, USD/CNY = 7.1
10. **Динамические курсы**: Остальное из интернета

### Мои добавления (9.1–9.10):
- Память и проактивные подсказки
- Ценовая аналитика и сравнение поставщиков
- Шаблоны заказов
- Вечерний дайджест
- Цели и KPI
- CRM-Lite с рейтингами
- Дашборд
- Умные триггеры (курс, молчание поставщика, баланс)
- Чек-листы для процессов
- Мульти-склад

---

**Документ утверждён. Система готова к сборке.**
**Следующий шаг: Создание «Ядра» — БД + Конфигурация + Универсальный парсер.**
